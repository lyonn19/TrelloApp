<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TrelloApp.Views.ViewCardFromList"
             Title="Cards List">
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="Save" Text="New Card" Icon="ic_stat_add.png" Priority="0" Order="Primary" Clicked="NewCard_Clicked"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="ListViewCardList"
                ItemsSource="{Binding CardsList}"
                ItemSelected="ListView_OnItemSelected"
                SeparatorVisibility="Default"
                BackgroundColor="Transparent">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}" Detail="{Binding Desc}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.Footer>
                    <StackLayout>
                        <Label Text="{Binding CardsList.Count, StringFormat='Cards: {0}'}" 
                               HorizontalOptions="End" TextColor="Blue"/>
                    </StackLayout>
                </ListView.Footer>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>